image/
__pycache__
**/build/
**/*.egg-info/
**/dist/
*.so
exp
weights
data
log
wandb
slurm_logs
outputs/
.vscode
.idea
.env
.venv
*/.DS_Store
**/*.out
/Dockerfile
**/exp/
scripts/slurm/**
notebooks/
benchmarks/
# autoresearch artifacts
results.tsv
run.log

# checkpoints / array dumps (never commit binaries)
*.pth
*.ckpt
*.npz

# third-party clones under libs/ — ignore everything except the vendored,
# tracked build libs (new libs are ignored by default; add a negation to track)
libs/*
!libs/cnms/
!libs/pointgroup_ops/
!libs/pointops/
!libs/pointops2/
!libs/pointrope/
!libs/pytorch3d_ops/
!libs/serialize_cuda/

# agent / assistant tooling (don't commit to the repo)
.claude/
.codex
.agents/
AGENT.md

# local scratch / private notes
ablations.csv
WORKFLOW-private-fork.md
*.scratch

tools/
tests/
scripts/
!/.github/scripts/
!/.github/scripts/**